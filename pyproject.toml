[project]
name = "todo-cli"
version = "0.1.0"
description = "A simple CLI todo manager."
authors = [{name = "Sonic"}]
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}

[project.scripts]
# This creates the CLI command `todo` after installation
todo = "todo.cli:main"
