[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "todo-cli"
version = "0.1.0"
description = "Simple todo CLI"
requires-python = ">=3.10"

[project.scripts]
todo = "todo.cli:main"
